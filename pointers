#include <iostream>

using namespace std;


void func(int *p , int k){
    *p = *p + 1;
    k = k + 1;
    
}

//void dummy(int arr[] , int n){
 //   cout<<sizeof(arr)<<endl;  // pointer has 4 bit sizw
//}

int main()
{
    int a = 5;
   // int *p = &a;
    int k = 8 ;
  //  cout<<p<<endl;  // 100 which is addresh of a
 //   cout<<*p<<endl; // * 
  //  cout<<k<<endl;
    /*
    cout<<p<<endl;  // 100 which is addresh of a
    cout<<*p<<endl; // * IS DEREFRENCE OPERATOR it return the value of main value pf pointer which is 5 here
    cout<<&p<<endl; // 96 which is addresh of p
    cout<<a<<endl;  // 5
    cout<<&a<<endl; // 100 which is addresh of a
    cout<<*a<<endl; // error
    
    cout<<sizeof(p);
*/

//func(p,k);
//cout<<p<<endl;  // 100 which is addresh of a
   // cout<<*p<<endl; // * 6
  //  cout<<k<<endl;
    
    
      int arr[] = {1,2,3,4,5};
   /* 
    cout<<arr<<endl; // addresh of base arr[0] self reference
    cout<<&arr<<endl; //  addresh of arrsy is arr[0]
    cout<<*arr<<endl;
     cout<<arr[0]<<endl;
    cout<<&arr[0]<<endl;
    cout<<*(arr + 1)<<endl;
     cout<<*(arr + 3)<<endl;
      cout<<*(arr + 2)<<endl;
       cout<<*(arr + 5)<<endl;
       int i = 2;
       cout<<i[arr]<<endl; // strange na 
       
    */
    //dummy(arr, 5);
    //  cout<<sizeof(arr)<<endl; // 1 int has 4 bit sizw
      
      
      //char pointer
      
      char ch = 'A';
      char *p = &ch;
      cout<<*p<<endl;
        cout<<p<<endl; 
        
      char cha[] = "abcd";
      cout<<*cha<<endl;
      cout<<cha<<endl; // entire string print here which is dif from int . it run till it fint nul char
    return 0;
}


